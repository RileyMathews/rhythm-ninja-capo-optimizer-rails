version: '3.7'
services:
  dev:
    build: 
      context: .
      dockerfile: ./.docker/dev/dockerfile
    tty: true
    stdin_open: true
    ports: 
      - "3000:3000"
    volumes: 
      - .:/app:delegated
      - bundle:/bundle

volumes: 
  bundle:
